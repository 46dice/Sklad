import { Text, View } from 'react-native'
import { FC } from 'react'

type Props = {}

export const Agents: FC<Props> = () => {
	return (
		<View>
			<Text className='text-white'>Agents</Text>
		</View>
	)
}
